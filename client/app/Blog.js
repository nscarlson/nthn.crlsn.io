import React from 'react';
import ReactDOM from 'react-dom';
import { Router } from 'react-router';
//import routes from './config/BlogRoutes';

ReactDOM.render(
  //<Router>{routes}</Router>,
  document.getElementById('nthncrlsn')
)
